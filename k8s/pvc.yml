apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qa-bot-data
  namespace: qa
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi        # тесты + отчёты + registry.json
  storageClassName: standard  # замени на свой StorageClass если нужно
