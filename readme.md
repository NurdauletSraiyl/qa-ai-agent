# ─────────────────────────────────────────────────────────────────────────────
# QA AI Agent — VPS deploy script
# Запускать на свежем Ubuntu 22.04 сервере:
#   bash deploy.sh
# ─────────────────────────────────────────────────────────────────────────────

## В случае самостоятельного поднятия без автоматического запуска через файл deploy.sh:
    1. Склонировать репо к себе локально
    2. Инициализировать гит
    3. По примеру с .env.example создать собсвтенный .env и подключить токен, токен бота и собсвтенный id
    4. Опционально поставить ссылку на BASE_URL для тестирования
    5. Запустить docker compose build --no-cache (при повторном запуске можно не указывать флаг --no-cache для более быстрого деплоя)
    6. Проверить логи на правильность запуска docker compose logs --tail 50 -f

# ─────────────────────────────────────────────────────────────────────────────
# QA AI Agent — VPS deploy script
# Запускать на свежем Ubuntu 22.04 сервере:
#   bash deploy.sh
# ─────────────────────────────────────────────────────────────────────────────

# 🤖 QA AI Agent

**QA AI Agent** — мощный Telegram-бот для автоматизации процессов тестирования (QA) и роботизации рутинных задач (RPA).

Бот интегрирован с искусственным интеллектом для генерации автотестов «на лету» и использует невидимый браузер (Playwright) для выполнения E2E-сценариев и выписки страховых полисов прямо через UI.

---

## 🚀 Основные возможности

| | Возможность | Описание |
|:---:|---|---|
| 🧠 | **AI-генерация тестов** | Автоматическое написание и запуск Playwright-тестов по текстовому описанию фичи |
| 🔁 | **Регрессионное тестирование** | Пакетный запуск сгенерированных тестов и отслеживание статусов (*Сломано / Починено*) |
| 📊 | **Excel & PDF отчёты** | Генерация наглядных отчётов по результатам прогона тестов |
| 🔍 | **Анализ багов** | AI-инспекция логов ошибок с подсказками по их исправлению |
| 📡 | **API-интеграция** | Быстрый поиск и получение данных по страховым полисам (ОГПО, НС, МСТ) через REST API |
| 🤖 | **RPA (Web Automation)** | Автоматическая выписка полисов через веб-интерфейс (эмуляция действий пользователя) со скриншотами |

---

## 🛠 Технологический стек

| Слой | Технологии |
|---|---|
| **Backend** | Node.js, Telegraf (Telegram Bot API) |
| **QA & RPA** | Playwright (Chromium) |
| **AI** | Anthropic API (Claude) |
| **Инфраструктура** | Docker, Docker Compose |

---

## ⚙️ Развёртывание (Deployment)

### Вариант 1. Автоматический деплой на VPS (Ubuntu 22.04)

Если вы поднимаете бота на чистом сервере, достаточно запустить bash-скрипт:

```bash
bash deploy.sh
```

### Вариант 2. Ручная установка (Local / Custom VPS)

Для самостоятельного развёртывания без использования `deploy.sh`:

**1. Склонировать репозиторий:**

```bash
git clone <url_вашего_репозитория>
cd qa-ai-agent
```

**2. Инициализировать git** *(если необходимо)*:

```bash
git init
```

**3. Настроить переменные окружения.**

Скопируйте пример файла конфигурации и заполните его своими данными:

```bash
cp .env.example .env
```

> ⚠️ Обязательно укажите токен бота, AI-токен, ваш Telegram ID для доступа и креды от тестовых стендов (см. таблицу ниже).

**4. Сборка и запуск через Docker.**

При первом запуске рекомендуется использовать `--build` (или `--no-cache` для полностью чистой сборки):

```bash
docker compose up -d --build
```

**5. Проверка логов.**

Убедитесь, что контейнер успешно стартовал и браузер инициализировался:

```bash
docker compose logs --tail 50 -f
```

---

## 🔐 Переменные окружения (`.env`)

Для корректной работы бота убедитесь, что в файле `.env` заполнены следующие параметры:

| Переменная | Описание |
|---|---|
| `BOT_TOKEN` | Токен вашего Telegram-бота (получить у [@BotFather](https://t.me/BotFather)) |
| `ALLOWED_USERS` | Ваш Telegram ID (или список через запятую) для защиты бота от посторонних |
| `ANTHROPIC_API_KEY` | Ключ доступа к API Anthropic (Claude) для генерации тестов |
| `MYNDP_LOGIN` | Логин (username/login) от тестового стенда для RPA и API-авторизации |
| `MYNDP_PASSWORD` | Пароль от тестового стенда |
| `BASE_URL` | *(Опционально)* Базовый URL для тестирования |

---

## 📱 Команды бота

Бот поддерживает маршрутизацию команд в стиле консольного интерфейса.

### 🧠 Тестирование и AI

| Команда | Назначение |
|---|---|
| `/test <url> <фича>` | Сгенерировать AI-тест (Playwright), запустить его и выдать отчёт |
| `/test list` | Показать список всех сгенерированных тестов (ID) |
| `/test run <ID>` | Запустить конкретный тест по его ID (например, `TC-001`) |
| `/test retry <ID>` | Перезапустить только упавшие шаги в тесте |
| `/regress [url]` | Запустить регресс-прогон всех последних тестов |
| `/checklist <url> <фича>` | Сгенерировать чек-лист проверок для фичи |
| `/investigate <лог>` | AI-анализ причины падения теста |

### 🌐 Работа со страховыми продуктами

| Команда | Назначение |
|---|---|
| `/policy ns list` | Получить список полисов НС по API |
| `/policy mst <ИИН>` | Найти полисы МСТ по ИИН пользователя через API |
| `/policy ogpo <номер>` | Получить детали конкретного полиса ОГПО |

### 🤖 RPA (выписка через Web)

| Команда | Назначение |
|---|---|
| `/web ogpo <ИИН> <Госномер>` | Робот открывает браузер, авторизуется, вводит ИИН и госномер авто, выпускает ОГПО со скриншотами |
| `/web mst <ИИН>` | Роботизированная выписка МСТ *(в разработке)* |